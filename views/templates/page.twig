{% extends 'layouts/base.twig' %}

{% block content %}
    <article class="post-type-{{ post.type }}" id="post-{{ post.id }}">
        <h2>{{ post.title }}</h2>

        <section>
            {{ post.content }}
        </section>
    </article>
{% endblock %}
