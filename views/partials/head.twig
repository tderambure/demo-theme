<head>
    <meta charset="{{ site.charset }}" />
    <link rel="stylesheet" href="{{ site.theme.link }}/dist/css/demo-theme.css" type="text/css" media="screen" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />

    {% do action('get_header') %}
    {{ function('wp_head') }}
</head>
